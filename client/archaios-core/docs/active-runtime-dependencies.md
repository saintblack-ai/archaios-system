# Active Runtime Dependencies

Generated: 2026-05-30

Mode: documentation only. No moves, deletions, renames, archival, or workflow edits.

## Active Execution Surfaces

| Surface | Entry | Actively executed folders | Classification |
| --- | --- | --- | --- |
| Cloudflare Worker backend | `worker.js` via `wrangler.toml` | `agents/`, `shared/` | ACTIVE PRODUCTION |
| Vite frontend | `client/src/main.jsx` via `client/package.json` | `client/src/`, `client/src/agents/`, `client/src/api/`, `client/src/lib/` | ACTIVE PRODUCTION |
| Local ARCHAIOS runtime | `client/archaios-core/orchestrator/orchestrator.mjs` via `client/package.json` | `client/archaios-core/agents/`, `client/archaios-core/runtime/`, `client/archaios-core/state/`, `client/tasks/`, `client/logs/`, `client/projects/`, `client/knowledge/`, `client/revenue/` | ACTIVE PRODUCTION for local runtime |
| Scheduled Cloudflare agents | `archaios-agents/src/index.ts` via `archaios-agents/wrangler.toml` | `archaios-agents/src/`, `archaios-agents/sql/` | NEEDS MIGRATION / repair before production confidence |
| Express local server | `server/index.js` via `server/package.json` and root helper scripts | `server/`, `server/lib/`, `server/data/`, `shared/` | ACTIVE DEV / fallback backend |
| Root Next app | `app/` via root `package.json` | `app/`, `middleware.ts`, `next.config.js` | NEEDS DECISION |
| Archaios OS | Python/Streamlit/local workflows | `Archaios OS/`, `ArchaiosControl/` | NEEDS MIGRATION / internal-only |

## Agent Folder Execution Status

| Folder | Evidence | Status | Classification |
| --- | --- | --- | --- |
| `agents/` | Direct imports from `worker.js` lines 1-7 | Actively executed by production Worker | ACTIVE PRODUCTION |
| `client/src/agents/` | Imported by `client/src/App.jsx`, `Dashboard.jsx`, `PricingPage.jsx`, `BookGrowthCommand.jsx` | Bundled into active Vite frontend | ACTIVE PRODUCTION |
| `client/archaios-core/agents/` | Dispatched by `client/archaios-core/orchestrator/orchestrator.mjs`; referenced by runtime manifests/service registry | Canonical local runtime agents | ACTIVE PRODUCTION for local runtime |
| `archaios-agents/src/` | Worker package with `GET /api/agents/status`; package has deploy/dev/check scripts | Intended scheduled Cloudflare worker, but dependency/deploy health needs repair | NEEDS MIGRATION |
| `client/agents/archaios/` | Generated by `client/scripts/archaios-ingest-build.mjs`; no package script dispatch found | Duplicate generated agent copy | NEEDS MIGRATION |
| `client/agents/marketing/`, `client/agents/infra-agent/`, `client/agents/product-agent/`, `client/agents/revenue-agent/` | README/manifest docs only | Documentation/mission indexes | SAFE TO ARCHIVE after docs consolidation |
| `app/agents/` | Imported by `app/scheduler/agentScheduler.ts` and app routes; root Next scripts exist | Active only if root Next app is retained | NEEDS DECISION |
| `archaios/agents/` | Python package imports inside `archaios/core/orchestrator.py` | Legacy Python runtime | NEEDS MIGRATION |
| `Archaios OS/agents/` | Python/Streamlit internal operator docs and jobs | Internal local operator layer | NEEDS MIGRATION |
| `ARCHAIOS_Codex_AI_Agent_Pack/` | Self-contained legacy agent pack | No active production import found | SAFE TO ARCHIVE after preservation |
| `Author_AI/`, `Executor_AI/`, `Mentor_AI/`, `Scholar_AI/` | Imported by root wrapper files `author_agent.py`, `executor_agent.py`, `mentor_agent.py`, `scholar_agent.py` | Legacy role-agent wrappers | SAFE TO ARCHIVE after wrapper decision |

## Dependency Edges

Worker/backend:

```text
worker.js
  -> agents/*.js
  -> shared/pricing.js
  -> OpenAI API
  -> Supabase REST/Auth
  -> Stripe API
```

Frontend:

```text
client/src/main.jsx
  -> client/src/App.jsx
    -> client/src/agents/alertCenter.js
      -> client/src/agents/marketAgent.js
      -> client/src/agents/newsAgent.js
      -> client/src/agents/sitrepAgent.js
      -> client/src/lib/alertsRepository.js
    -> client/src/agents/stripeAgent.js
      -> client/src/lib/platform.js
```

Book Growth frontend module:

```text
client/src/pages/bookGrowth/BookGrowthCommand.jsx
  -> client/src/agents/bookGrowth/index.js
    -> analyticsAgent.js
    -> architectMode.js
    -> brandStoryAgent.js
    -> contentCreatorAgent.js
    -> contentQuality.js
    -> emailCommunityAgent.js
    -> funnelAgent.js
    -> operationsAgent.js
    -> salesOptimizerAgent.js
    -> seoMetadataAgent.js
    -> socialDistributionAgent.js
    -> strategyCommander.js
```

Canonical local runtime:

```text
client/package.json
  -> npm run archaios:orchestrate
    -> client/archaios-core/orchestrator/orchestrator.mjs
      -> client/archaios-core/runtime/agents/*.json
      -> client/archaios-core/agents/*.js
      -> client/tasks/
      -> client/logs/
      -> client/projects/
      -> client/knowledge/
      -> client/revenue/
```

Next app:

```text
package.json
  -> next dev / next build / next start
    -> app/
    -> middleware.ts
    -> next.config.js
    -> app/lib/*
    -> app/api/*
    -> app/agents/*
```

## Documentation Inconsistencies Found

These are recommendations only, not changes made in this phase:

- `client/scripts/archaios-ingest-build.mjs` still generates `client/agents/archaios/*.js` and says to run core agents from `agents/archaios/`.
- `client/docs/ARCHAIOS_INFRASTRUCTURE_MAP.md` says `agents/` is the runtime agent path, but canonical runtime is now `client/archaios-core/agents/`.
- Several docs still say `npm run build` without clarifying whether it means root Next build or `client` Vite build.
- Root `package.json` still makes `npm run build` mean `next build`.
