# ARCHAIOS Orchestrator Layer

`orchestrator.mjs` provides:

- central task routing and dispatch
- queue state transitions (intake -> in progress -> completed/blocked)
- service registry updates
- dashboard data model output generation
