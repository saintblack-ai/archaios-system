# SaaS Tier System (Free / Pro / Elite)

- Infrastructure note: tier-aware dashboard/service gating with test-mode checkout entry points.
- Dependencies:
1. stable auth state resolver
2. Stripe test price IDs and webhook signature validation
3. subscription sync endpoint validation
- Build readiness state: build-ready (test-prep)
- Assigned agent: Revenue Agent (primary), Product Agent (support)
