# Archaios Next 90 Days

## Highest ROI Actions

1. Restore Vercel Supabase configuration, redeploy, and prove the authenticated dashboard journey.
2. Re-deploy and verify the intended `archaios-saas-worker`; require health identity and release checks for every release.
3. Determine whether the configured Supabase project is recoverable; restore or replace only through the canonical migration manifest.
4. Run one Stripe test-mode checkout through the correct Worker and reconcile it to Supabase.
5. Personally acquire and support the first qualified user using the existing Pro/Elite offer.
6. Establish monthly encrypted exports, Git mirrors, Notion exports, and a verified restoration drill.

## Launch Blockers

- Vercel Production lacks `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`; `/dashboard` returns `500`.
- The live Workers.dev endpoint serves `archaios-daily-automation`, not the intended `archaios-saas-worker` identity.
- The configured Supabase host is unresolved from independent health checks; project account status is unknown.
- Stripe product, price, webhook, subscription synchronization, and checkout have not been verified end to end.
- Customer-facing production authority is split between GitHub Pages and Vercel without a single confirmed live journey.

## Revenue Actions

1. Complete the recovery gates in sequence: Supabase, Worker identity, authenticated dashboard, Stripe test checkout, webhook, subscription persistence.
2. Recruit one qualified existing-audience user for a direct, observed onboarding session.
3. Convert the first active user through the existing Pro or Elite checkout only after the test transaction proves the path.
4. Track signup, activation, checkout start, paid conversion, MRR, payment failure, and retention from verified sources.
5. Repeat the direct founder sales process until ten real customers; retain only acquisition channels that produce paid users.

## What Not To Work On

- New products, agents, dashboards, routes, tiers, integrations, or redesigns.
- Custom domain purchase or DNS changes.
- AI feature expansion before the existing paid path works.
- Broad marketing spend before one customer has completed payment and achieved value.
- Legacy SQL/schema expansion outside the canonical migration manifest.
- Reorganizing or deleting the dirty workspace before backups and intentional commits exist.
