# Memory Knowledge

This directory stores memory summaries, retrieval manifests, pruning records, and vector-index metadata.

Memory entries should include source references, timestamp, sensitivity, summary, embedding status, and retention policy.
